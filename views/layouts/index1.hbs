
    <div class="row">
        <div class="col-md-3">
            {{> chat-toolbar}}
            {{> dice-toolbar}}
        </div>
        <div class="col-md-6">
            <h1>{{title}}</h1>
            <p>Prepare to {{title}}</p>
            {{> user-toolbar}}
            {{> konva-stage}}
            {{> experience-toolbar}}
        </div>
        <div class="col-md-3 col-lg-3">
            {{> token-toolbar}}
            {{> map-toolbar}}
            {{> clear-layers}}
        </div>
    </div>
    <div>
    <div hidden>
        {{> user-experience-toolbar}}
    </div>







